# API base URL (leave blank to use production automatically)
EXPO_PUBLIC_API_URL=

# RevenueCat public SDK key (iOS — used in Purchases.configure())
EXPO_PUBLIC_REVENUECAT_API_KEY=test_PREuNDTIcRujtiIEpLMJmHlkAdO

# RevenueCat secret key — server-side only (webhooks, server-to-server API)
# NEVER prefix with EXPO_PUBLIC_ — must never be bundled into the app
REVENUECAT_SECRET_KEY=
